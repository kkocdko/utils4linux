#!/bin/sh
# if pidof chrome > /dev/null; then
# echo skip set text-scaling-factor
# else
# gsettings set org.gnome.desktop.interface text-scaling-factor 1
# nohup sh -c 'sleep 1.5 ; gsettings set org.gnome.desktop.interface text-scaling-factor 1.25' > /dev/null &
# sleep 0.1
# fi
# cd /home/kkocdko/misc/apps/google-chrome
cd /media/kkocdko/KK_TMP_1/google-chrome
exec ./chrome \
--disable-logging \
--disable-speech-api \
--disable-domain-reliability \
--disable-smooth-scrolling \
--disable-quic \
--disable-chrome-tracing-computation \
--disable-crash-reporter \
--disable-renderer-accessibility \
--disable-top-sites \
--disable-component-update \
--check-for-update-interval=315360000 \
--disable-features=TabHoverCardImages,EyeDropper,AIPromptAPI,LanguageDetectionAPI,TranslationAPI,ExtensionManifestV2Unsupported,ExtensionManifestV2Disabled \
--extension-mime-request-handling=always-prompt-for-install \
--password-store=basic \
--force-device-scale-factor=1 \
--gtk-version=4 \
--force-local-ntp \
--use-angle=vulkan \
--enable-zero-copy \
--ui-enable-zero-copy \
--enable-features=FluentOverlayScrollbar,Vulkan,VulkanFromANGLE,DefaultANGLEVulkan,AcceleratedVideoEncoder,AcceleratedVideoDecoder,AcceleratedVideoDecodeLinuxGL,VaapiIgnoreDriverChecks,VaapiVideoDecoder,VaapiVideoEncoder,VaapiVideoEncodeAccelerator,UseMultiPlaneFormatForHardwareVideo \
--google-url=https://cp.cloudflare.com \
--google-apis-url=https://cp.cloudflare.com \
--google-base-url=https://cp.cloudflare.com \
--google-doodle-url=https://cp.cloudflare.com \
--search-provider-logo-url=https://cp.cloudflare.com \
--secure-connect-api-url=https://cp.cloudflare.com \
--optimization-guide-service-get-models-url=https://cp.cloudflare.com \
--user-data-dir=/media/kkocdko/KK_TMP_1/home_config/google-chrome \
--disk-cache-dir=/media/kkocdko/KK_TMP_1/home_cache/google-chrome \
"$@" > /dev/null 2>&1

exit 1

# optimizationguide-pa.googleapis.com
# --disable-crashpad-for-testing
# --enable-unsafe-webgpu
# --ignore-gpu-blocklist
# --enable-skia-graphite
# SkiaGraphite:dawn_skip_validation/true
# SkiaGraphite

# todo: avoid ~/.config/google-chrome/screen_ai

mkdir -p /etc/opt/chrome/policies/managed
curl -o /etc/opt/chrome/policies/managed/managed_policies.json -L https://github.com/corbindavenport/just-the-browser/raw/refs/heads/main/chrome/managed_policies.json

cat <<EOF > /usr/share/applications/google-chrome.desktop
[Desktop Entry]
Version=1.0
Name=Google Chrome
Exec=/home/kkocdko/misc/apps/google-chrome/main %U
Terminal=false
Type=Application
Categories=Network;WebBrowser;
MimeType=application/pdf;application/rdf+xml;application/rss+xml;application/xhtml+xml;application/xhtml_xml;application/xml;image/gif;image/jpeg;image/png;image/webp;text/html;text/xml;x-scheme-handler/http;x-scheme-handler/https;
EOF
update-desktop-database

mv chrome_crashpad_handler chrome_crashpad_handler_origin
printf '#!/bin/sh\nexec ./chrome_crashpad_handler_origin --database /media/kkocdko/KK_TMP_1/home_config/google-chrome-crash "$@"\n' > chrome_crashpad_handler
chmod +x chrome_crashpad_handler

# stable # 145.0.7632.159
mkdir google-chrome
cd google-chrome
curl -o google-chrome.deb -L https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
ar -p google-chrome.deb data.tar.xz | tar -xJ
mv opt/google/chrome/* .
rm -rf usr etc opt cron default_apps WidevineCdm MEIPreload PrivacySandboxAttestationsPreloaded CHROME_VERSION_* *.png *.xpm xdg-* liboptimization_guide_internal.so google-chrome chrome-management-service default-app-block locales/{ml,ta,kn,te,hi,bn,gu,mr,th,el}.pak

# https://github.com/chromium/chromium/blob/141.0.7356.1/components/optimization_guide/core/optimization_guide_features.cc
# https://raw.githubusercontent.com/chromium/chromium/refs/tags/141.0.7348.1/gpu/config/gpu_switches.cc

